[pytest]
# 自动日志格式
log_format = %(asctime)s - %(name)s - %(levelname)s - %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

# 日志级别
log_level = INFO

# 日志文件配置（由 conftest.py 动态设置）
log_file_level = INFO
log_file_format = %(asctime)s - %(levelname)s - %(filename)s - %(message)s
