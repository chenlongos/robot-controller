#### ARM

机械臂目前有六关节和三关节两种。六关节是so101，使用lekiwi控制器。三关节使用的普通舵机，使用自己的代码控制每个舵机的角度。

机械臂的硬件层仅提供最底层的关节级控制，笛卡尔坐标移动、抓取动作等高级控制由上层控制层实现。抽象接口定义见 [2_4抽象层设计.md](file:///home/radxa/aka01b/docs/2_4抽象层设计.md)。

##### SO101六轴机械臂

```python
# src/arm/so101_arm.py
from typing import List, Dict, Tuple
from src.abstract.arm_interface import ArmInterface


class SO101Arm(ArmInterface):
    """SO101六轴机械臂实现
    
    使用Feetech电机总线控制六关节机械臂。
    """
    
    JOINT_NAMES: Tuple[str, ...] = (
        "shoulder_pan",
        "shoulder_lift", 
        "elbow_flex",
        "wrist_flex",
        "wrist_roll",
    )
    
    def __init__(self, config: Dict):
        """
        Args:
            config: 配置字典，包含以下关键字:
                - port: 串口端口路径 (默认: "/dev/ttyUSB0")
                - use_degrees: 是否使用角度模式 (默认: False)
                - disable_torque_on_disconnect: 断开连接时是否禁用扭矩 (默认: True)
                - max_relative_target: 最大相对目标位置 (可选)
                - calibration: 校准数据 (可选)
        """
        pass
    
    @property
    def is_connected(self) -> bool:
        """检查机械臂是否已连接"""
        pass
    
    def connect(self, calibrate: bool = True) -> None:
        """连接机械臂"""
        pass
    
    def disconnect(self) -> None:
        """断开连接"""
        pass
    
    def move_to_joint_positions(self, positions: List[float]) -> None:
        """移动到关节位置"""
        pass
    
    def get_joint_positions(self) -> List[float]:
        """获取当前关节位置"""
        pass
    
    def set_gripper_position(self, position: float) -> None:
        """设置夹爪位置"""
        pass
    
    def get_gripper_position(self) -> float:
        """获取夹爪位置"""
        pass
    
    def is_moving(self) -> bool:
        """检查是否正在移动"""
        pass
    
    def stop(self) -> None:
        """停止运动"""
        pass
```

##### ZP10S三轴机械臂

```python
# src/arm/zp10s_arm.py
from typing import List, Dict, Tuple
from src.abstract.arm_interface import ArmInterface


class ZP10SArm(ArmInterface):
    """ZP10S三轴机械臂实现
    
    使用串口控制三关节机械臂，使用普通舵机。
    """
    
    JOINT_NAMES: Tuple[str, ...] = (
        "servo0",
        "servo1",
    )
    
    def __init__(self, config: Dict):
        """
        Args:
            config: 配置字典，包含以下关键字:
                - port: 串口端口路径 (默认: "/dev/ttyS7")
                - baudrate: 波特率 (默认: 115200)
                - timeout: 超时时间 (默认: 0.1)
        """
        pass
    
    @property
    def is_connected(self) -> bool:
        """检查机械臂是否已连接"""
        pass
    
    def connect(self, calibrate: bool = True) -> None:
        """连接机械臂
        
        Args:
            calibrate: 是否执行校准 (默认: True，ZP10S不使用校准)
        """
        pass
    
    def disconnect(self) -> None:
        """断开连接"""
        pass
    
    def move_to_joint_positions(self, positions: List[float]) -> None:
        """移动到关节位置"""
        pass
    
    def get_joint_positions(self) -> List[float]:
        """获取当前关节位置
        
        Returns:
            两个关节的位置列表
            
        Note: ZP10S舵机不支持位置反馈，返回默认值。
        """
        pass
    
    def set_gripper_position(self, position: float) -> None:
        """设置夹爪位置"""
        pass
    
    def get_gripper_position(self) -> float:
        """获取夹爪位置
        
        Note: ZP10S舵机不支持位置反馈，返回默认值。
        """
        pass
    
    def is_moving(self) -> bool:
        """检查是否正在移动"""
        pass
    
    def stop(self) -> None:
        """停止运动"""
        pass
```

#### 底盘

底盘的类型从轮子的数量来看，有两轮， 三轮， 四轮。从轮子的类型来看，有差速轮和麦轮。

底盘把对每个电机的pwm进行控制，把控制层的线速度和角度速度转换为电机的pwm值。

aka01b机器人使用d24a驱动板（带4路tb6612模块），使用四个jgb37-520电机。

##### 电机控制

```python
# src/base/d24a_jgb37.py
from dataclasses import dataclass
from typing import Optional

@dataclass
class MotorConfig:
    """电机配置"""
    name: str
    in2: int              # PWM输入2
    in1: int              # PWM输入1
    phase_A: int          # 编码器相位A
    phase_B: int          # 编码器相位B
    pwm_chip: int         # PWM芯片编号
    pwm_channel: int      # PWM通道
    direction_inverted: bool = False  # 方向是否反转
    max_pwm: int = 100
    gear_ratio: float = 1.0

class EncoderCounter:
    """编码器计数器"""
    
    def __init__(self, pin_a: int, pin_b: int) -> None:
        pass
    
    def get_count(self) -> int:
        """获取编码器计数"""
        pass
    
    def reset(self) -> None:
        """重置计数器"""
        pass

class PIDController:
    """PID控制器"""
    
    def __init__(self, kp: float, ki: float, kd: float) -> None:
        pass
    
    def compute(self, error: float, dt: float) -> float:
        """计算PID输出"""
        pass
    
    def reset(self) -> None:
        """重置控制器状态"""
        pass

class Motor:
    """电机类"""
    
    def __init__(self, config: MotorConfig) -> None:
        pass
    
    def set_speed(self, speed: float) -> None:
        """设置电机速度 (-1.0 到 1.0)"""
        pass
    
    def stop(self) -> None:
        """停止电机"""
        pass
    
    def get_speed(self) -> float:
        """获取当前速度"""
        pass
    
    def cleanup(self) -> None:
        """清理资源"""
        pass
```

##### 差分底盘控制

```python
# src/base/differential_base.py
from src.abstract.base_interface import BaseInterface
from src.base.d24a_jgb37 import Motor, MotorConfig

class DifferentialBase(BaseInterface):
    """差分底盘实现
    
    差分底盘仅支持前后移动(x)和旋转(w)，左右移动(y)将被忽略。
    """
    
    def __init__(self, config: Dict) -> None:
        self.left_motor: Motor
        self.right_motor: Motor
        self.wheel_radius: float
        self.wheel_base: float
        self.max_speed: float
    
    def move(self, x: float, y: float, w: float) -> None:
        """控制底盘运动
        
        Args:
            x: 前后方向速度 (m/s)
            y: 左右方向速度 (m/s) - 差分底盘不支持，将被忽略
            w: 旋转角速度 (rad/s)
        """
        pass
    
    def stop(self) -> None:
        """停止运动"""
        pass
    
    def cleanup(self) -> None:
        """清理资源"""
        pass
```

##### 麦轮底盘控制
```python
# src/base/mecanum_base.py
from src.abstract.base_interface import BaseInterface
from src.base.d24a_jgb37 import Motor, MotorConfig

class MecanumBase(BaseInterface):
    """麦轮底盘实现
    
    麦轮底盘支持全向移动，包括前后(x)、左右(y)和旋转(w)。
    """
    
    def __init__(self, config: Dict) -> None:
        self.front_left_motor: Motor
        self.front_right_motor: Motor
        self.back_left_motor: Motor
        self.back_right_motor: Motor
        self.wheel_radius: float
        self.base_radius: float
        self.max_speed: float
    
    def move(self, x: float, y: float, w: float) -> None:
        """控制底盘运动
        
        Args:
            x: 前后方向速度 (m/s)
            y: 左右方向速度 (m/s)
            w: 旋转角速度 (rad/s)
        """
        pass
    
    def stop(self) -> None:
        """停止运动"""
        pass
    
    def cleanup(self) -> None:
        """清理资源"""
        pass
```

#### 相机控制

初始化硬件相机，向视觉模块提供图像数据。

```python
# src/camera/usb_camera.py
import cv2
import numpy as np
from src.abstract.camera_interface import CameraInterface

class USBCamera(CameraInterface):
    """USB摄像头实现"""
    
    def __init__(self, config: Dict) -> None:
        self.cap: cv2.VideoCapture
        self.device_id: int
        self.resolution: tuple
        self.fps: int
    
    def capture(self) -> np.ndarray:
        """捕获一帧图像"""
        pass
    
    def get_intrinsics(self) -> Dict[str, float]:
        """获取相机内参"""
        pass
    
    def release(self) -> None:
        """释放资源"""
        pass
    
    def is_opened(self) -> bool:
        """检查摄像头是否打开"""
        pass
```